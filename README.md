📌 Features
Loads an image (pic.jpg) from the project directory.

Converts the image to grayscale for better detection accuracy.

Uses Haar Cascade (haarcascade_frontalface_default.xml) to detect frontal faces.

Draws blue rectangles around each detected face.

Displays the processed image in a window.
