📌 Features

1. Loads an image (pic.jpg) from the project directory.

2. Converts the image to grayscale for better detection accuracy.

3. Uses Haar Cascade (haarcascade_frontalface_default.xml) to detect frontal faces.

4. Draws blue rectangles around each detected face.

5. Displays the processed image in a window.
